version: "3"
services:
  app:
    container_name: swapi
    image: swapi
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      - db


  db:
    image: postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: swapi
